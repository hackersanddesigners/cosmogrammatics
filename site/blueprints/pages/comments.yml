title: Comments

permissions:
  extends: options/permissions

sections:
  comments:
    label: Comments
    type: pages
    info: "{{ page.text }}"
    template: comment
    layout: table
    columns:
      block_id:
        label: Block ID
        value: '{{ page.block_id }}'
      selection_type:
        label: Selection type
        value: '{{ page.selection_type }}'
      
        
