label: Blocks
type: blocks

fieldsets:
  columns:
    extends: blocks/columns
    fields:
      layout:
        layouts:
          - "1/1"
          - "1/2, 1/2"
          - "1/4, 1/4, 1/4, 1/4"
        fieldsets:
          - columns
          - heading
          - quote
          - list
          - image
          - gallery

          text:
            extends: blocks/text
            fields:
              footnotes:
                label: Footnotes
                type: structure
                fields:
                  note:
                    label: Note
                    type: writer
                  ref:
                    label: Ref
                    type: text
                    default: '0'
                    disabled: true

          soundblock:
            name: Audio
            fields:
              soundfile:
                label: Audio (file)
                type: files
                options: audio
              soundurl:
                label: Audio (url)
                type: embed

           videoblock:
             name: Video
             snippet: videoblock
             fields:
               videofile:
                 label: Video (file)
                 type: files
                 options: videos
               videoposter:
                 label: Cover (file)
                 type: files
               videourl:
                 label: Video (url)
                 type: embed

  - heading
  - quote
  - list
  - image
  - gallery

  text:
    extends: blocks/text
    fields:
      footnotes:
        label: Footnotes
        type: structure
        fields:
          note:
            label: Footnote
            type: writer
          ref:
            label: Ref
            type: text
            default: '0'
            disabled: true

  soundblock:
    name: Audio
    fields:
      soundfile:
        label: Audio (file)
        type: files
        options: audio
      soundurl:
        label: Audio (url)
        type: embed

  videoblock:
    name: Video
    snippet: videoblock
    fields:
      videofile:
        label: Video (file)
        type: files
        options: videos
      videoposter:
        label: Cover (file)
        type: files
      videourl:
        label: Video (url)
        type: embed

settings:
  fields:
    bg_image:
      label: Backround Image
      type: files
    bg_color:
      label: Background Color
      type: colors