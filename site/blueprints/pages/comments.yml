title: Comments

sections:
  comments:
    label: Comments
    type: pages
    info: "{{ page.text }}"
    template: comment
    layout: table
    columns:
      slug:
        value: '{{ page.article_slug }}'
        