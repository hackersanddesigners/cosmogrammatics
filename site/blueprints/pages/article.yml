title: Article

columns:
  - width: 2/3
    fields:
      builder:
        label: Blocks
        type: blocks
        fieldsets:
          - heading
          - text
          - quote
          - list
          - image
          - gallery

          soundblock:
            name: Audio
            fields:
              soundfile:
                label: Audio (file)
                type: files
                options: audio
              soundurl:
                label: Audio (url)
                type: embed

          videoblock:
            name: Video
            snippet: videoblock
            fields:
              videofile:
                label: Video (file)
                type: files
                options: videos
              videoposter:
                label: Cover (file)
                type: files
              videourl:
                label: Video (url)
                type: embed

          footnote:
            name: Footnote
            fields:
              text:
                label: Text
                type: textarea

  - width: 1/3
    sections:
      files:
        label: Files
        type: files